# AGENTS.md

## プロジェクト

このリポジトリは、エージェントベース大規模世界シミュレーション「MachiVerse」の公式Webサイトを管理するためのリポジトリです。

本体の開発リポジトリは [`MachiVerse-Project/MachiVerse`](https://github.com/MachiVerse-Project/MachiVerse) です。

## 基本方針

1. ユーザーから明示的な実装指示がない限り、HTML、CSS、JavaScript、設定ファイル、画像参照、デプロイ設定その他の実装変更を開始しないこと。
2. 議論、要件整理、構成案、デザイン案、調査、見積もり、計画は、実装が明示的に依頼されない限り実装作業と分離すること。
3. リポジトリへ変更を加える前に、その変更がユーザーから許可された範囲に含まれることを確認すること。
4. MachiVerseについて、現在実装済みの内容、現在開発中の内容、長期的な構想を明確に区別して表現すること。
5. 長期構想や検討中の要素を、現在利用可能・実装済みであるかのように記載しないこと。
6. 技術的な説明、開発状況、ロードマップ、バージョン表記等について不確かな場合は、本体リポジトリの現行資料を確認し、推測で確定事項を作らないこと。
7. MachiVerseの紹介では、単なる機能列挙よりも「世界の現在だけでなく、そこへ至った因果や歴史も扱う」というプロジェクトの中心的な方向性を尊重すること。
8. 利用者ロール「ダイバー」等のMachiVerse固有用語を使用する場合は、本体プロジェクトで定義された意味から逸脱しないこと。

## 公開ディレクトリ

1. GitHub Pagesへ公開するWebルートは `www/` とする。
2. `www/` には、ブラウザへ配信するために必要なファイルだけを置くこと。
3. README、AGENTS、制作メモ、運用資料、原本画像、中間素材、未使用アセット、開発用設定などを `www/` に置かないこと。
4. 公開HTML、`404.html`、`CNAME`、`.nojekyll`、`robots.txt`、`sitemap.xml` は `www/` 直下で管理すること。
5. 公開CSS、JavaScript、画像はそれぞれ `www/assets/css/`、`www/assets/js/`、`www/assets/images/` で管理すること。
6. `www/assets/` へ追加する前に、現在の公開HTML、CSS、JavaScriptから実際に参照されるか確認すること。
7. アセットが未使用になった場合は、参照を確認したうえで `www/` から削除し、公開物を最小限に保つこと。
8. 相対パスは `www/` をWebルートとして成立すること。リポジトリルートから配信される前提の参照を作らないこと。

## ブランドとアセット

1. MachiVerseの名称、ロゴ、シンボル、ワードマークその他のブランド資産は、`TRADEMARKS.md` の方針に従って扱うこと。
2. 公式ブランドアセットの原本は、本体リポジトリの `promotion` ブランチ配下にある `assets/images/` を基準とすること。
3. Websiteリポジトリには、Web配信で実際に必要なアセットのみを保持し、原本や大型の中間素材を不要に複製しないこと。
4. 南雲澪の公開用画像は、原則として `www/assets/images/characters/nagumo-mio/web/` にあるWeb参照用ファイルだけを配信対象とし、ポーズ原本一式を `www/` に複製しないこと。
5. 南雲澪はMachiVerseの案内キャラクター（Guide Character）として扱う。実在の開発者、著者、運営主体であるかのような `SYSTEM DEVELOPMENT` 等の肩書きを付与しないこと。
6. Webサイト上の開発・運営主体は `MachiVerse Project` として本体リポジトリへ結び付け、案内キャラクターとプロジェクト主体を混同しないこと。
7. ロゴの縦横比、色、形状、シンボルとワードマークの構成や間隔を任意に変更しないこと。
8. 公式SymbolやWordmarkが用意されている用途では、フルロゴから独自に切り出した画像を作らないこと。
9. favicon、OG画像、SNS共有画像等を変更する場合は、サイト本体の参照とメタデータも合わせて確認すること。

## コンテンツ制作ルール

1. 一般向けの公式サイトとして、開発者向けの内部用語や実装詳細を必要以上に前面へ出さないこと。
2. 技術的な詳細が必要な場合は、本体リポジトリのREADME、Roadmap、Development Guide、設計文書等への導線を優先すること。
3. 誇張表現、未確認の性能値、未確定のリリース日、利用できない機能の利用可能性を断定しないこと。
4. ページ内で同じ概念を説明する場合、用語、トーン、MachiVerseの位置づけを一貫させること。
5. 日本語を主要言語として扱う。多言語対応を追加する場合は、ユーザーの明示的な方針決定なしに既存の日本語コンテンツを置き換えないこと。
6. 外部向けページでは、「世界を作る」のではなく「世界がそうなった理由までシミュレーションする」という方向性を損なわないこと。
7. 案内キャラクターによる演出と、プロジェクトの一次情報・開発主体・著者性が混同されない表現を維持すること。

## Web実装ルール

1. 現在のサイトは、`www/` 配下のHTML/CSS/JavaScriptをそのまま配信する、ビルド不要の静的サイトとして構成されている。この構成を変更する場合は、フレームワーク、ビルドツール、パッケージマネージャーその他の依存関係を無断で追加しないこと。
2. Astro、Next.js、Vite等のフレームワークやビルドシステムの導入は、ユーザーから明示的な承認を得てから行うこと。
3. 既存のレスポンシブ対応を維持し、デスクトップだけを前提とした実装にしないこと。
4. セマンティックHTML、キーボード操作、フォーカス表示、適切な代替テキスト、コントラスト等、基本的なアクセシビリティを維持すること。
5. 不要なJavaScriptを追加しないこと。静的HTML/CSSで十分な場合はそちらを優先すること。
6. 外部CDN、外部Webフォント、解析スクリプト、サードパーティSDK等を追加する場合は、目的とプライバシー・性能上の影響を確認し、無断で導入しないこと。
7. APIキー、トークン、認証情報、秘密鍵、個人情報、非公開設定等をリポジトリへコミットしないこと。
8. `.env` 等のローカル設定が必要になった場合は、秘密情報を含まない `.env.example` 等を用意し、実値はコミットしないこと。

## SEO・公開設定

1. `title`、`description`、canonical URL、Open Graph、Twitter Card、`robots.txt`、`sitemap.xml` 等の既存SEO設定を、理由なく削除・破壊しないこと。
2. 本番URLは `https://machiverse.app/` とする。
3. canonical URL、OG画像URL、サイトマップ等で本番URLを変更する場合は、関連箇所をまとめて確認すること。
4. `www/404.html`、`www/.nojekyll`、`www/CNAME` はGitHub Pages運用に関わるため、削除・移動する前に公開方式への影響を確認すること。
5. 現在の本番公開先はGitHub Pagesとし、`main` を本番状態として扱うこと。
6. GitHub Pagesは `.github/workflows/pages.yml` から `www/` のみをartifactとして公開すること。branch sourceの `/ (root)` や `/docs` を公開元として扱わないこと。
7. `index`、`developer`、`self-hosting`、`architecture` の各言語セットでは、各URLをself-canonicalとし、日本語URLへ一括canonicalしないこと。
8. 多言語ページは `ja` / `en` / `zh-TW` / `ko` / `x-default` の対応関係を維持し、`www/sitemap.xml` の相互 `hreflang` とページ側の実行時headが矛盾しないこと。
9. JSON-LDで著者・開発主体を表現する場合は `MachiVerse Project` を主体とし、南雲澪を実在開発者・著者として構造化しないこと。
10. `.github/workflows/pages.yml` のSEO検証を維持し、meta description、self-canonical、favicon、OGP、robots、sitemapの欠落をデプロイ前に検出すること。

## ブランチ運用ルール

### 常設ブランチ

- `main`: 本番・公開可能な状態を保持する。GitHub Pagesの本番内容に対応する。
- `develop`: 次回公開予定の変更を統合する開発ブランチ。

### 通常の開発フロー

通常の変更は、原則として次の流れで行う。

```text
feature/*
   ↓
develop
   ↓
main
```

1. 通常の機能追加、デザイン変更、コンテンツ追加、修正は `feature/*` ブランチで行うこと。
2. `develop` は統合用ブランチとして扱い、通常の作業を直接積み重ねないこと。
3. `main` は本番ブランチとして扱い、通常の開発作業を直接行わないこと。
4. ただし、ユーザーが初回セットアップ、緊急修正、設定変更等について明示的に `main` への直接変更を指示した場合は、その指示を優先すること。
5. `main` に直接変更を行った場合、必要に応じて `develop` との同期状態を確認し、差分をユーザーへ明示すること。

## 変更時の確認事項

変更後は、変更内容に応じて可能な範囲で次を確認すること。

- `www/` 直下に公開不要な管理ファイルが混入していないこと
- `www/assets/` に未使用の原本・中間素材を持ち込んでいないこと
- HTMLの構造が破損していないこと
- CSS参照、JavaScript参照、画像参照、内部リンク、外部リンクが `www/` をルートとして意図したパスになっていること
- モバイル幅で明らかなレイアウト崩れを生まないこと
- favicon、OG画像、canonical URL等の参照が有効であること
- `machiverse.app` を前提とする公開設定と矛盾していないこと
- 実装済み機能と長期構想の表現が混同されていないこと
- 案内キャラクターとMachiVerse Projectの役割が混同されていないこと
- 多言語ページのself-canonicalと `hreflang` 対応が崩れていないこと
- `LICENSE` および `TRADEMARKS.md` の扱いと矛盾していないこと
- `.github/workflows/pages.yml` が `www/` だけをPages artifactとして扱っていること

## ライセンスと商標

このリポジトリのソースコードおよび資料の利用条件は `LICENSE` を参照すること。

MachiVerseの名称、ロゴその他のブランド利用については `TRADEMARKS.md` を参照すること。

オープンソースライセンスによりソースコードの利用が認められていることと、MachiVerseの名称やロゴを自由にブランド利用できることは同義ではない。

## ルールの更新

Webサイト制作・公開・ブランド運用に関するプロジェクト固有のルールが追加された場合は、この `AGENTS.md` へ追記し、後続の作業から参照できる状態を保つこと。
